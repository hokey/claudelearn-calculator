---
name: build
description: Compile the TypeScript project
allowed-tools: Bash
---

Run `npm run build` to compile TypeScript to JavaScript in the `dist/` directory.

If the build succeeds and the user wants to run the app, run `npm start`.
